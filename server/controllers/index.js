const Auth = require('./auth');
const Content = require('./content');
const Question = require('./question');
const Subject = require('./subject');
const User = require('./user');

module.exports = { Auth, Content, Question, Subject, User };
