class User {
    constructor(id, name, picture, socketId) {
        this.id = id;
        this.name = name;
        this.picture = picture;
        this.socketId = socketId;
    }
}

export default User;
